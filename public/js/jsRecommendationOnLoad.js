/*
function isEmpty()
{
  var firstname = document.querySelector('[name="first_name"]').value;
  var lastname = document.querySelector('[name="last_name"]').value;
  var email = document.querySelector('[name="email"]').value;
  var password = document.querySelector('[name="password"]').value;
  var number = document.querySelector('[name="number"]').value;
  if (!firstname && !lastname && !email
    && !password  && !number) {
    return 1;
  }
  else
  {
    return 0;
  }
}
myId = setInterval(function poppingOut()
{
  var recommendationShowed = 0;
  if (isEmpty() && recommendationShowed != 2)
  {
            window.open("#ModalWindow","_self");
            recommendationShowed += 1;
            clearTimeout(timerId);
  }

},delay=10000,10000)
*/
